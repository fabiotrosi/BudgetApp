@model (string Url, string Text)
<a href="@Model.Url" style="display:inline-block; background-color:#0d6efd; color:#ffffff; text-decoration:none; padding:12px 24px; border-radius:6px; font-weight:bold; font-size:15px;">@Model.Text</a>
